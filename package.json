{
  "name": "redpacket-web3",
  "version": "1.0.0",
  "description": "Decentralized Blockchain Red Packet Application",
  "scripts": {
    "start": "concurrently \"npm run node\" \"npm run frontend\"",
    "node": "npx hardhat node",
    "frontend": "cd dapp && npm run dev",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "concurrently": "^7.6.0"
  }
}
